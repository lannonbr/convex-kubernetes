convexSha: 25d170103019e63d6a90602ca821c8c48e6713a5
hostBasePath: ""

ingress:
  className: "tailscale"
  tls: true
  annotations: {}
  backend:
    host: ""     # hostname for ingress routing (e.g. "convex-backend" for Tailscale, "api.example.com" for others)
    origin: ""   # optional: full FQDN for env vars if different from host (e.g. "convex-backend.my-tailnet.ts.net")
  site:
    host: ""     # hostname for ingress routing (e.g. "convex-site" for Tailscale, "site.example.com" for others)
    origin: ""   # optional: full FQDN for env vars if different from host
  dashboard:
    host: ""     # hostname for ingress routing (e.g. "convex-dashboard" for Tailscale, "dashboard.example.com" for others)
    origin: ""   # optional: full FQDN for env vars if different from host
